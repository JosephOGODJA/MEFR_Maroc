The clip_adder.sh add all file in the main_part_1.py script and add the concatenate python line to the second file.
It then generate the main_render.py python video edition script and run it.

First to generate the pre-render video named "render.mp4" and secondly add the logo to the file with the add_logo.py file.
